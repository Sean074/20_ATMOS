# ATMOS 1.0.0

**Author:** Sean O'Meara

A Python TUI for standard atmosphere calculations and speed conversions.

**Capabilities:**
- Atmospheric property lookup by altitude or static pressure
- Speed conversions between Mach, KTAS, KCAS, and KEAS at a given altitude
- Speed-altitude chart generation (KTAS, KCAS, KEAS) with optional speed envelopes

---

## Setup

**1. Create a virtual environment and install dependencies:**

```bash
python3 -m venv .venv
source .venv/bin/activate        # macOS / Linux
# .venv\Scripts\activate         # Windows
pip install -r requirements.txt
```

**2. Run the application:**

```bash
python main.py
```

On startup, select an atmospheric model CSV from `data/models/`. The main menu then drives all operations.

---

## How it works

All calculation inputs come from JSON files in `data/inputs/` — the TUI asks which file to run rather than prompting for individual values. Speed envelope JSON files in `data/envelopes/` configure chart axis ranges and boundary overlays.

Input file schemas and envelope JSON structure are documented in [CLAUDE.md](CLAUDE.md).

---

## Atmospheric models

Two models ship in `data/models/`:

| File | Source |
|---|---|
| `standard_atmos.csv` | Nelson, *Flight Stability and Automatic Control* |
| `standard_atmos_1976.csv` | US Standard Atmosphere 1976 (generated by `tools/gen_ussa76.py`) |

---

## Configuration

Edit `config/defaults.json` to adjust chart axis ranges, grid lines, and solver tolerances without touching code. See [CLAUDE.md](CLAUDE.md) for the key listing.

---

## Reference

| Document | Contents |
|---|---|
| [doc/architecture.md](doc/architecture.md) | Layer diagram, module responsibilities, dependency rules, data flow |
| [doc/analysis_code.md](doc/analysis_code.md) | Variable naming conventions for computation modules |
| [doc/ui.md](doc/ui.md) | TUI code standards (Rich/prompt_toolkit palette, panel/table conventions) |
| [tools/atmos_methods.ipynb](tools/atmos_methods.ipynb) | Worked examples and method derivations for all physics calculations |
